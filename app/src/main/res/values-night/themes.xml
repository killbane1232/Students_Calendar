<resources xmlns:tools="http://schemas.android.com/tools">
    <declare-styleable name="VerticalView">
        <attr name="calendarIcon" format="dimension" />
        <attr name="textColor" format="color" />
        <attr name="textBold" format="boolean" />
    </declare-styleable>
    <!-- Base application theme. -->
    <style name="Theme.Students_Calendar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="calendarIcon">@drawable/ic_calendar_icon_black</item>
        <item name="noteIcon">@drawable/ic_note_icon_black</item>
        <item name="menuIcon">@drawable/ic_menu_icon_black</item>
    </style>
</resources>